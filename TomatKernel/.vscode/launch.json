{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request": "attach",
            "executable": "./kernel.elf",
            "target": ":1234",
            "remote": true,
            "preLaunchTask": "Run With Debugger",
            "cwd": "${workspaceRoot}"
        }
    ]
}