In ubuntu after installing guest addition

sudo mount -t vboxsf OS OS

make sure to create the OS directory under HOME

The first parameter after the vboxsf is the directory on the machine undef HOME
here it is OS

The second operand in the above command (OS) is the name of the shared folder in the 
vistaulbox Shared Folders settings e.g.